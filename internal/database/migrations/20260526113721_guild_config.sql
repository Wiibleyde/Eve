-- Create "guild_configs" table
CREATE TABLE "guild_configs" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "guild_id" character varying NOT NULL, "key" character varying NOT NULL, "value" character varying NOT NULL, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, PRIMARY KEY ("id"));
-- Create index "guildconfig_guild_id_key" to table: "guild_configs"
CREATE UNIQUE INDEX "guildconfig_guild_id_key" ON "guild_configs" ("guild_id", "key");
