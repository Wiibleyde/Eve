// Code generated by ent, DO NOT EDIT.

package ent

import (
	"Eve/internal/database/ent/activequiz"
	"Eve/internal/database/ent/predicate"
	"Eve/internal/database/ent/quizquestion"
	"Eve/internal/database/ent/quizuseranswer"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ActiveQuizQuery is the builder for querying ActiveQuiz entities.
type ActiveQuizQuery struct {
	config
	ctx             *QueryContext
	order           []activequiz.OrderOption
	inters          []Interceptor
	predicates      []predicate.ActiveQuiz
	withQuestion    *QuizQuestionQuery
	withUserAnswers *QuizUserAnswerQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ActiveQuizQuery builder.
func (_q *ActiveQuizQuery) Where(ps ...predicate.ActiveQuiz) *ActiveQuizQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ActiveQuizQuery) Limit(limit int) *ActiveQuizQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ActiveQuizQuery) Offset(offset int) *ActiveQuizQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ActiveQuizQuery) Unique(unique bool) *ActiveQuizQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ActiveQuizQuery) Order(o ...activequiz.OrderOption) *ActiveQuizQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryQuestion chains the current query on the "question" edge.
func (_q *ActiveQuizQuery) QueryQuestion() *QuizQuestionQuery {
	query := (&QuizQuestionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(activequiz.Table, activequiz.FieldID, selector),
			sqlgraph.To(quizquestion.Table, quizquestion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, activequiz.QuestionTable, activequiz.QuestionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUserAnswers chains the current query on the "user_answers" edge.
func (_q *ActiveQuizQuery) QueryUserAnswers() *QuizUserAnswerQuery {
	query := (&QuizUserAnswerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(activequiz.Table, activequiz.FieldID, selector),
			sqlgraph.To(quizuseranswer.Table, quizuseranswer.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, activequiz.UserAnswersTable, activequiz.UserAnswersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ActiveQuiz entity from the query.
// Returns a *NotFoundError when no ActiveQuiz was found.
func (_q *ActiveQuizQuery) First(ctx context.Context) (*ActiveQuiz, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{activequiz.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ActiveQuizQuery) FirstX(ctx context.Context) *ActiveQuiz {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ActiveQuiz ID from the query.
// Returns a *NotFoundError when no ActiveQuiz ID was found.
func (_q *ActiveQuizQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{activequiz.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ActiveQuizQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ActiveQuiz entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ActiveQuiz entity is found.
// Returns a *NotFoundError when no ActiveQuiz entities are found.
func (_q *ActiveQuizQuery) Only(ctx context.Context) (*ActiveQuiz, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{activequiz.Label}
	default:
		return nil, &NotSingularError{activequiz.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ActiveQuizQuery) OnlyX(ctx context.Context) *ActiveQuiz {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ActiveQuiz ID in the query.
// Returns a *NotSingularError when more than one ActiveQuiz ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ActiveQuizQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{activequiz.Label}
	default:
		err = &NotSingularError{activequiz.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ActiveQuizQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ActiveQuizs.
func (_q *ActiveQuizQuery) All(ctx context.Context) ([]*ActiveQuiz, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ActiveQuiz, *ActiveQuizQuery]()
	return withInterceptors[[]*ActiveQuiz](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ActiveQuizQuery) AllX(ctx context.Context) []*ActiveQuiz {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ActiveQuiz IDs.
func (_q *ActiveQuizQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(activequiz.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ActiveQuizQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ActiveQuizQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ActiveQuizQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ActiveQuizQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ActiveQuizQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ActiveQuizQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ActiveQuizQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ActiveQuizQuery) Clone() *ActiveQuizQuery {
	if _q == nil {
		return nil
	}
	return &ActiveQuizQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]activequiz.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.ActiveQuiz{}, _q.predicates...),
		withQuestion:    _q.withQuestion.Clone(),
		withUserAnswers: _q.withUserAnswers.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithQuestion tells the query-builder to eager-load the nodes that are connected to
// the "question" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ActiveQuizQuery) WithQuestion(opts ...func(*QuizQuestionQuery)) *ActiveQuizQuery {
	query := (&QuizQuestionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withQuestion = query
	return _q
}

// WithUserAnswers tells the query-builder to eager-load the nodes that are connected to
// the "user_answers" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ActiveQuizQuery) WithUserAnswers(opts ...func(*QuizUserAnswerQuery)) *ActiveQuizQuery {
	query := (&QuizUserAnswerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUserAnswers = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		QuestionID string `json:"question_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ActiveQuiz.Query().
//		GroupBy(activequiz.FieldQuestionID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ActiveQuizQuery) GroupBy(field string, fields ...string) *ActiveQuizGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ActiveQuizGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = activequiz.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		QuestionID string `json:"question_id,omitempty"`
//	}
//
//	client.ActiveQuiz.Query().
//		Select(activequiz.FieldQuestionID).
//		Scan(ctx, &v)
func (_q *ActiveQuizQuery) Select(fields ...string) *ActiveQuizSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ActiveQuizSelect{ActiveQuizQuery: _q}
	sbuild.label = activequiz.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ActiveQuizSelect configured with the given aggregations.
func (_q *ActiveQuizQuery) Aggregate(fns ...AggregateFunc) *ActiveQuizSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ActiveQuizQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !activequiz.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ActiveQuizQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ActiveQuiz, error) {
	var (
		nodes       = []*ActiveQuiz{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withQuestion != nil,
			_q.withUserAnswers != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ActiveQuiz).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ActiveQuiz{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withQuestion; query != nil {
		if err := _q.loadQuestion(ctx, query, nodes, nil,
			func(n *ActiveQuiz, e *QuizQuestion) { n.Edges.Question = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUserAnswers; query != nil {
		if err := _q.loadUserAnswers(ctx, query, nodes,
			func(n *ActiveQuiz) { n.Edges.UserAnswers = []*QuizUserAnswer{} },
			func(n *ActiveQuiz, e *QuizUserAnswer) { n.Edges.UserAnswers = append(n.Edges.UserAnswers, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ActiveQuizQuery) loadQuestion(ctx context.Context, query *QuizQuestionQuery, nodes []*ActiveQuiz, init func(*ActiveQuiz), assign func(*ActiveQuiz, *QuizQuestion)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ActiveQuiz)
	for i := range nodes {
		fk := nodes[i].QuestionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(quizquestion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "question_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ActiveQuizQuery) loadUserAnswers(ctx context.Context, query *QuizUserAnswerQuery, nodes []*ActiveQuiz, init func(*ActiveQuiz), assign func(*ActiveQuiz, *QuizUserAnswer)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*ActiveQuiz)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(quizuseranswer.FieldActiveQuizID)
	}
	query.Where(predicate.QuizUserAnswer(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(activequiz.UserAnswersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ActiveQuizID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "active_quiz_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ActiveQuizQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ActiveQuizQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(activequiz.Table, activequiz.Columns, sqlgraph.NewFieldSpec(activequiz.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, activequiz.FieldID)
		for i := range fields {
			if fields[i] != activequiz.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withQuestion != nil {
			_spec.Node.AddColumnOnce(activequiz.FieldQuestionID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ActiveQuizQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(activequiz.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = activequiz.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ActiveQuizGroupBy is the group-by builder for ActiveQuiz entities.
type ActiveQuizGroupBy struct {
	selector
	build *ActiveQuizQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ActiveQuizGroupBy) Aggregate(fns ...AggregateFunc) *ActiveQuizGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ActiveQuizGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActiveQuizQuery, *ActiveQuizGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ActiveQuizGroupBy) sqlScan(ctx context.Context, root *ActiveQuizQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ActiveQuizSelect is the builder for selecting fields of ActiveQuiz entities.
type ActiveQuizSelect struct {
	*ActiveQuizQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ActiveQuizSelect) Aggregate(fns ...AggregateFunc) *ActiveQuizSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ActiveQuizSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ActiveQuizQuery, *ActiveQuizSelect](ctx, _s.ActiveQuizQuery, _s, _s.inters, v)
}

func (_s *ActiveQuizSelect) sqlScan(ctx context.Context, root *ActiveQuizQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
