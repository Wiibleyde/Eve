// Code generated by ent, DO NOT EDIT.

package ent

import (
	"Eve/internal/database/ent/lotogame"
	"Eve/internal/database/ent/lotoplayer"
	"Eve/internal/database/ent/lototicket"
	"Eve/internal/database/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LotoTicketQuery is the builder for querying LotoTicket entities.
type LotoTicketQuery struct {
	config
	ctx        *QueryContext
	order      []lototicket.OrderOption
	inters     []Interceptor
	predicates []predicate.LotoTicket
	withGame   *LotoGameQuery
	withPlayer *LotoPlayerQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LotoTicketQuery builder.
func (_q *LotoTicketQuery) Where(ps ...predicate.LotoTicket) *LotoTicketQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *LotoTicketQuery) Limit(limit int) *LotoTicketQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *LotoTicketQuery) Offset(offset int) *LotoTicketQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *LotoTicketQuery) Unique(unique bool) *LotoTicketQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *LotoTicketQuery) Order(o ...lototicket.OrderOption) *LotoTicketQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryGame chains the current query on the "game" edge.
func (_q *LotoTicketQuery) QueryGame() *LotoGameQuery {
	query := (&LotoGameClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lototicket.Table, lototicket.FieldID, selector),
			sqlgraph.To(lotogame.Table, lotogame.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lototicket.GameTable, lototicket.GameColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPlayer chains the current query on the "player" edge.
func (_q *LotoTicketQuery) QueryPlayer() *LotoPlayerQuery {
	query := (&LotoPlayerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lototicket.Table, lototicket.FieldID, selector),
			sqlgraph.To(lotoplayer.Table, lotoplayer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lototicket.PlayerTable, lototicket.PlayerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LotoTicket entity from the query.
// Returns a *NotFoundError when no LotoTicket was found.
func (_q *LotoTicketQuery) First(ctx context.Context) (*LotoTicket, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lototicket.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *LotoTicketQuery) FirstX(ctx context.Context) *LotoTicket {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LotoTicket ID from the query.
// Returns a *NotFoundError when no LotoTicket ID was found.
func (_q *LotoTicketQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lototicket.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *LotoTicketQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LotoTicket entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LotoTicket entity is found.
// Returns a *NotFoundError when no LotoTicket entities are found.
func (_q *LotoTicketQuery) Only(ctx context.Context) (*LotoTicket, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lototicket.Label}
	default:
		return nil, &NotSingularError{lototicket.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *LotoTicketQuery) OnlyX(ctx context.Context) *LotoTicket {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LotoTicket ID in the query.
// Returns a *NotSingularError when more than one LotoTicket ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *LotoTicketQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lototicket.Label}
	default:
		err = &NotSingularError{lototicket.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *LotoTicketQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LotoTickets.
func (_q *LotoTicketQuery) All(ctx context.Context) ([]*LotoTicket, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LotoTicket, *LotoTicketQuery]()
	return withInterceptors[[]*LotoTicket](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *LotoTicketQuery) AllX(ctx context.Context) []*LotoTicket {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LotoTicket IDs.
func (_q *LotoTicketQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(lototicket.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *LotoTicketQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *LotoTicketQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*LotoTicketQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *LotoTicketQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *LotoTicketQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *LotoTicketQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LotoTicketQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *LotoTicketQuery) Clone() *LotoTicketQuery {
	if _q == nil {
		return nil
	}
	return &LotoTicketQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]lototicket.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.LotoTicket{}, _q.predicates...),
		withGame:   _q.withGame.Clone(),
		withPlayer: _q.withPlayer.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithGame tells the query-builder to eager-load the nodes that are connected to
// the "game" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoTicketQuery) WithGame(opts ...func(*LotoGameQuery)) *LotoTicketQuery {
	query := (&LotoGameClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGame = query
	return _q
}

// WithPlayer tells the query-builder to eager-load the nodes that are connected to
// the "player" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoTicketQuery) WithPlayer(opts ...func(*LotoPlayerQuery)) *LotoTicketQuery {
	query := (&LotoPlayerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPlayer = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GameID string `json:"game_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LotoTicket.Query().
//		GroupBy(lototicket.FieldGameID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *LotoTicketQuery) GroupBy(field string, fields ...string) *LotoTicketGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LotoTicketGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = lototicket.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GameID string `json:"game_id,omitempty"`
//	}
//
//	client.LotoTicket.Query().
//		Select(lototicket.FieldGameID).
//		Scan(ctx, &v)
func (_q *LotoTicketQuery) Select(fields ...string) *LotoTicketSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &LotoTicketSelect{LotoTicketQuery: _q}
	sbuild.label = lototicket.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LotoTicketSelect configured with the given aggregations.
func (_q *LotoTicketQuery) Aggregate(fns ...AggregateFunc) *LotoTicketSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *LotoTicketQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !lototicket.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *LotoTicketQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LotoTicket, error) {
	var (
		nodes       = []*LotoTicket{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withGame != nil,
			_q.withPlayer != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LotoTicket).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LotoTicket{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withGame; query != nil {
		if err := _q.loadGame(ctx, query, nodes, nil,
			func(n *LotoTicket, e *LotoGame) { n.Edges.Game = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPlayer; query != nil {
		if err := _q.loadPlayer(ctx, query, nodes, nil,
			func(n *LotoTicket, e *LotoPlayer) { n.Edges.Player = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *LotoTicketQuery) loadGame(ctx context.Context, query *LotoGameQuery, nodes []*LotoTicket, init func(*LotoTicket), assign func(*LotoTicket, *LotoGame)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LotoTicket)
	for i := range nodes {
		fk := nodes[i].GameID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lotogame.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "game_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *LotoTicketQuery) loadPlayer(ctx context.Context, query *LotoPlayerQuery, nodes []*LotoTicket, init func(*LotoTicket), assign func(*LotoTicket, *LotoPlayer)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LotoTicket)
	for i := range nodes {
		fk := nodes[i].PlayerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lotoplayer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "player_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *LotoTicketQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *LotoTicketQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lototicket.Table, lototicket.Columns, sqlgraph.NewFieldSpec(lototicket.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lototicket.FieldID)
		for i := range fields {
			if fields[i] != lototicket.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withGame != nil {
			_spec.Node.AddColumnOnce(lototicket.FieldGameID)
		}
		if _q.withPlayer != nil {
			_spec.Node.AddColumnOnce(lototicket.FieldPlayerID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *LotoTicketQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(lototicket.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = lototicket.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LotoTicketGroupBy is the group-by builder for LotoTicket entities.
type LotoTicketGroupBy struct {
	selector
	build *LotoTicketQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *LotoTicketGroupBy) Aggregate(fns ...AggregateFunc) *LotoTicketGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *LotoTicketGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotoTicketQuery, *LotoTicketGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *LotoTicketGroupBy) sqlScan(ctx context.Context, root *LotoTicketQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LotoTicketSelect is the builder for selecting fields of LotoTicket entities.
type LotoTicketSelect struct {
	*LotoTicketQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *LotoTicketSelect) Aggregate(fns ...AggregateFunc) *LotoTicketSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *LotoTicketSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotoTicketQuery, *LotoTicketSelect](ctx, _s.LotoTicketQuery, _s, _s.inters, v)
}

func (_s *LotoTicketSelect) sqlScan(ctx context.Context, root *LotoTicketQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
