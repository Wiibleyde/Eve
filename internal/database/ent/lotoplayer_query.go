// Code generated by ent, DO NOT EDIT.

package ent

import (
	"Eve/internal/database/ent/lotogame"
	"Eve/internal/database/ent/lotoplayer"
	"Eve/internal/database/ent/lotoprize"
	"Eve/internal/database/ent/lototicket"
	"Eve/internal/database/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LotoPlayerQuery is the builder for querying LotoPlayer entities.
type LotoPlayerQuery struct {
	config
	ctx           *QueryContext
	order         []lotoplayer.OrderOption
	inters        []Interceptor
	predicates    []predicate.LotoPlayer
	withGame      *LotoGameQuery
	withTickets   *LotoTicketQuery
	withWonPrizes *LotoPrizeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LotoPlayerQuery builder.
func (_q *LotoPlayerQuery) Where(ps ...predicate.LotoPlayer) *LotoPlayerQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *LotoPlayerQuery) Limit(limit int) *LotoPlayerQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *LotoPlayerQuery) Offset(offset int) *LotoPlayerQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *LotoPlayerQuery) Unique(unique bool) *LotoPlayerQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *LotoPlayerQuery) Order(o ...lotoplayer.OrderOption) *LotoPlayerQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryGame chains the current query on the "game" edge.
func (_q *LotoPlayerQuery) QueryGame() *LotoGameQuery {
	query := (&LotoGameClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotoplayer.Table, lotoplayer.FieldID, selector),
			sqlgraph.To(lotogame.Table, lotogame.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lotoplayer.GameTable, lotoplayer.GameColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTickets chains the current query on the "tickets" edge.
func (_q *LotoPlayerQuery) QueryTickets() *LotoTicketQuery {
	query := (&LotoTicketClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotoplayer.Table, lotoplayer.FieldID, selector),
			sqlgraph.To(lototicket.Table, lototicket.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, lotoplayer.TicketsTable, lotoplayer.TicketsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWonPrizes chains the current query on the "won_prizes" edge.
func (_q *LotoPlayerQuery) QueryWonPrizes() *LotoPrizeQuery {
	query := (&LotoPrizeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotoplayer.Table, lotoplayer.FieldID, selector),
			sqlgraph.To(lotoprize.Table, lotoprize.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, lotoplayer.WonPrizesTable, lotoplayer.WonPrizesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LotoPlayer entity from the query.
// Returns a *NotFoundError when no LotoPlayer was found.
func (_q *LotoPlayerQuery) First(ctx context.Context) (*LotoPlayer, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lotoplayer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *LotoPlayerQuery) FirstX(ctx context.Context) *LotoPlayer {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LotoPlayer ID from the query.
// Returns a *NotFoundError when no LotoPlayer ID was found.
func (_q *LotoPlayerQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lotoplayer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *LotoPlayerQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LotoPlayer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LotoPlayer entity is found.
// Returns a *NotFoundError when no LotoPlayer entities are found.
func (_q *LotoPlayerQuery) Only(ctx context.Context) (*LotoPlayer, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lotoplayer.Label}
	default:
		return nil, &NotSingularError{lotoplayer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *LotoPlayerQuery) OnlyX(ctx context.Context) *LotoPlayer {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LotoPlayer ID in the query.
// Returns a *NotSingularError when more than one LotoPlayer ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *LotoPlayerQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lotoplayer.Label}
	default:
		err = &NotSingularError{lotoplayer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *LotoPlayerQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LotoPlayers.
func (_q *LotoPlayerQuery) All(ctx context.Context) ([]*LotoPlayer, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LotoPlayer, *LotoPlayerQuery]()
	return withInterceptors[[]*LotoPlayer](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *LotoPlayerQuery) AllX(ctx context.Context) []*LotoPlayer {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LotoPlayer IDs.
func (_q *LotoPlayerQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(lotoplayer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *LotoPlayerQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *LotoPlayerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*LotoPlayerQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *LotoPlayerQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *LotoPlayerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *LotoPlayerQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LotoPlayerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *LotoPlayerQuery) Clone() *LotoPlayerQuery {
	if _q == nil {
		return nil
	}
	return &LotoPlayerQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]lotoplayer.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.LotoPlayer{}, _q.predicates...),
		withGame:      _q.withGame.Clone(),
		withTickets:   _q.withTickets.Clone(),
		withWonPrizes: _q.withWonPrizes.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithGame tells the query-builder to eager-load the nodes that are connected to
// the "game" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoPlayerQuery) WithGame(opts ...func(*LotoGameQuery)) *LotoPlayerQuery {
	query := (&LotoGameClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGame = query
	return _q
}

// WithTickets tells the query-builder to eager-load the nodes that are connected to
// the "tickets" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoPlayerQuery) WithTickets(opts ...func(*LotoTicketQuery)) *LotoPlayerQuery {
	query := (&LotoTicketClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTickets = query
	return _q
}

// WithWonPrizes tells the query-builder to eager-load the nodes that are connected to
// the "won_prizes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoPlayerQuery) WithWonPrizes(opts ...func(*LotoPrizeQuery)) *LotoPlayerQuery {
	query := (&LotoPrizeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWonPrizes = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GameID string `json:"game_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LotoPlayer.Query().
//		GroupBy(lotoplayer.FieldGameID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *LotoPlayerQuery) GroupBy(field string, fields ...string) *LotoPlayerGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LotoPlayerGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = lotoplayer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GameID string `json:"game_id,omitempty"`
//	}
//
//	client.LotoPlayer.Query().
//		Select(lotoplayer.FieldGameID).
//		Scan(ctx, &v)
func (_q *LotoPlayerQuery) Select(fields ...string) *LotoPlayerSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &LotoPlayerSelect{LotoPlayerQuery: _q}
	sbuild.label = lotoplayer.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LotoPlayerSelect configured with the given aggregations.
func (_q *LotoPlayerQuery) Aggregate(fns ...AggregateFunc) *LotoPlayerSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *LotoPlayerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !lotoplayer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *LotoPlayerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LotoPlayer, error) {
	var (
		nodes       = []*LotoPlayer{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withGame != nil,
			_q.withTickets != nil,
			_q.withWonPrizes != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LotoPlayer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LotoPlayer{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withGame; query != nil {
		if err := _q.loadGame(ctx, query, nodes, nil,
			func(n *LotoPlayer, e *LotoGame) { n.Edges.Game = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTickets; query != nil {
		if err := _q.loadTickets(ctx, query, nodes,
			func(n *LotoPlayer) { n.Edges.Tickets = []*LotoTicket{} },
			func(n *LotoPlayer, e *LotoTicket) { n.Edges.Tickets = append(n.Edges.Tickets, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withWonPrizes; query != nil {
		if err := _q.loadWonPrizes(ctx, query, nodes,
			func(n *LotoPlayer) { n.Edges.WonPrizes = []*LotoPrize{} },
			func(n *LotoPlayer, e *LotoPrize) { n.Edges.WonPrizes = append(n.Edges.WonPrizes, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *LotoPlayerQuery) loadGame(ctx context.Context, query *LotoGameQuery, nodes []*LotoPlayer, init func(*LotoPlayer), assign func(*LotoPlayer, *LotoGame)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LotoPlayer)
	for i := range nodes {
		fk := nodes[i].GameID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lotogame.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "game_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *LotoPlayerQuery) loadTickets(ctx context.Context, query *LotoTicketQuery, nodes []*LotoPlayer, init func(*LotoPlayer), assign func(*LotoPlayer, *LotoTicket)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*LotoPlayer)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(lototicket.FieldPlayerID)
	}
	query.Where(predicate.LotoTicket(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(lotoplayer.TicketsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PlayerID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "player_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *LotoPlayerQuery) loadWonPrizes(ctx context.Context, query *LotoPrizeQuery, nodes []*LotoPlayer, init func(*LotoPlayer), assign func(*LotoPlayer, *LotoPrize)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*LotoPlayer)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(lotoprize.FieldWinnerPlayerID)
	}
	query.Where(predicate.LotoPrize(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(lotoplayer.WonPrizesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WinnerPlayerID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "winner_player_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *LotoPlayerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *LotoPlayerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lotoplayer.Table, lotoplayer.Columns, sqlgraph.NewFieldSpec(lotoplayer.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lotoplayer.FieldID)
		for i := range fields {
			if fields[i] != lotoplayer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withGame != nil {
			_spec.Node.AddColumnOnce(lotoplayer.FieldGameID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *LotoPlayerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(lotoplayer.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = lotoplayer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LotoPlayerGroupBy is the group-by builder for LotoPlayer entities.
type LotoPlayerGroupBy struct {
	selector
	build *LotoPlayerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *LotoPlayerGroupBy) Aggregate(fns ...AggregateFunc) *LotoPlayerGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *LotoPlayerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotoPlayerQuery, *LotoPlayerGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *LotoPlayerGroupBy) sqlScan(ctx context.Context, root *LotoPlayerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LotoPlayerSelect is the builder for selecting fields of LotoPlayer entities.
type LotoPlayerSelect struct {
	*LotoPlayerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *LotoPlayerSelect) Aggregate(fns ...AggregateFunc) *LotoPlayerSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *LotoPlayerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotoPlayerQuery, *LotoPlayerSelect](ctx, _s.LotoPlayerQuery, _s, _s.inters, v)
}

func (_s *LotoPlayerSelect) sqlScan(ctx context.Context, root *LotoPlayerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
