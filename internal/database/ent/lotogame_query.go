// Code generated by ent, DO NOT EDIT.

package ent

import (
	"Eve/internal/database/ent/lotogame"
	"Eve/internal/database/ent/lotoplayer"
	"Eve/internal/database/ent/lotoprize"
	"Eve/internal/database/ent/lototicket"
	"Eve/internal/database/ent/predicate"
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LotoGameQuery is the builder for querying LotoGame entities.
type LotoGameQuery struct {
	config
	ctx         *QueryContext
	order       []lotogame.OrderOption
	inters      []Interceptor
	predicates  []predicate.LotoGame
	withPlayers *LotoPlayerQuery
	withTickets *LotoTicketQuery
	withPrizes  *LotoPrizeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LotoGameQuery builder.
func (_q *LotoGameQuery) Where(ps ...predicate.LotoGame) *LotoGameQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *LotoGameQuery) Limit(limit int) *LotoGameQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *LotoGameQuery) Offset(offset int) *LotoGameQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *LotoGameQuery) Unique(unique bool) *LotoGameQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *LotoGameQuery) Order(o ...lotogame.OrderOption) *LotoGameQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryPlayers chains the current query on the "players" edge.
func (_q *LotoGameQuery) QueryPlayers() *LotoPlayerQuery {
	query := (&LotoPlayerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotogame.Table, lotogame.FieldID, selector),
			sqlgraph.To(lotoplayer.Table, lotoplayer.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, lotogame.PlayersTable, lotogame.PlayersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTickets chains the current query on the "tickets" edge.
func (_q *LotoGameQuery) QueryTickets() *LotoTicketQuery {
	query := (&LotoTicketClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotogame.Table, lotogame.FieldID, selector),
			sqlgraph.To(lototicket.Table, lototicket.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, lotogame.TicketsTable, lotogame.TicketsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPrizes chains the current query on the "prizes" edge.
func (_q *LotoGameQuery) QueryPrizes() *LotoPrizeQuery {
	query := (&LotoPrizeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotogame.Table, lotogame.FieldID, selector),
			sqlgraph.To(lotoprize.Table, lotoprize.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, lotogame.PrizesTable, lotogame.PrizesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LotoGame entity from the query.
// Returns a *NotFoundError when no LotoGame was found.
func (_q *LotoGameQuery) First(ctx context.Context) (*LotoGame, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lotogame.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *LotoGameQuery) FirstX(ctx context.Context) *LotoGame {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LotoGame ID from the query.
// Returns a *NotFoundError when no LotoGame ID was found.
func (_q *LotoGameQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lotogame.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *LotoGameQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LotoGame entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LotoGame entity is found.
// Returns a *NotFoundError when no LotoGame entities are found.
func (_q *LotoGameQuery) Only(ctx context.Context) (*LotoGame, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lotogame.Label}
	default:
		return nil, &NotSingularError{lotogame.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *LotoGameQuery) OnlyX(ctx context.Context) *LotoGame {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LotoGame ID in the query.
// Returns a *NotSingularError when more than one LotoGame ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *LotoGameQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lotogame.Label}
	default:
		err = &NotSingularError{lotogame.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *LotoGameQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LotoGames.
func (_q *LotoGameQuery) All(ctx context.Context) ([]*LotoGame, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LotoGame, *LotoGameQuery]()
	return withInterceptors[[]*LotoGame](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *LotoGameQuery) AllX(ctx context.Context) []*LotoGame {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LotoGame IDs.
func (_q *LotoGameQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(lotogame.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *LotoGameQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *LotoGameQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*LotoGameQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *LotoGameQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *LotoGameQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *LotoGameQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LotoGameQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *LotoGameQuery) Clone() *LotoGameQuery {
	if _q == nil {
		return nil
	}
	return &LotoGameQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]lotogame.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.LotoGame{}, _q.predicates...),
		withPlayers: _q.withPlayers.Clone(),
		withTickets: _q.withTickets.Clone(),
		withPrizes:  _q.withPrizes.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithPlayers tells the query-builder to eager-load the nodes that are connected to
// the "players" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoGameQuery) WithPlayers(opts ...func(*LotoPlayerQuery)) *LotoGameQuery {
	query := (&LotoPlayerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPlayers = query
	return _q
}

// WithTickets tells the query-builder to eager-load the nodes that are connected to
// the "tickets" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoGameQuery) WithTickets(opts ...func(*LotoTicketQuery)) *LotoGameQuery {
	query := (&LotoTicketClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTickets = query
	return _q
}

// WithPrizes tells the query-builder to eager-load the nodes that are connected to
// the "prizes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotoGameQuery) WithPrizes(opts ...func(*LotoPrizeQuery)) *LotoGameQuery {
	query := (&LotoPrizeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPrizes = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GuildID string `json:"guild_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LotoGame.Query().
//		GroupBy(lotogame.FieldGuildID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *LotoGameQuery) GroupBy(field string, fields ...string) *LotoGameGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LotoGameGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = lotogame.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GuildID string `json:"guild_id,omitempty"`
//	}
//
//	client.LotoGame.Query().
//		Select(lotogame.FieldGuildID).
//		Scan(ctx, &v)
func (_q *LotoGameQuery) Select(fields ...string) *LotoGameSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &LotoGameSelect{LotoGameQuery: _q}
	sbuild.label = lotogame.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LotoGameSelect configured with the given aggregations.
func (_q *LotoGameQuery) Aggregate(fns ...AggregateFunc) *LotoGameSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *LotoGameQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !lotogame.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *LotoGameQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LotoGame, error) {
	var (
		nodes       = []*LotoGame{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withPlayers != nil,
			_q.withTickets != nil,
			_q.withPrizes != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LotoGame).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LotoGame{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withPlayers; query != nil {
		if err := _q.loadPlayers(ctx, query, nodes,
			func(n *LotoGame) { n.Edges.Players = []*LotoPlayer{} },
			func(n *LotoGame, e *LotoPlayer) { n.Edges.Players = append(n.Edges.Players, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTickets; query != nil {
		if err := _q.loadTickets(ctx, query, nodes,
			func(n *LotoGame) { n.Edges.Tickets = []*LotoTicket{} },
			func(n *LotoGame, e *LotoTicket) { n.Edges.Tickets = append(n.Edges.Tickets, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPrizes; query != nil {
		if err := _q.loadPrizes(ctx, query, nodes,
			func(n *LotoGame) { n.Edges.Prizes = []*LotoPrize{} },
			func(n *LotoGame, e *LotoPrize) { n.Edges.Prizes = append(n.Edges.Prizes, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *LotoGameQuery) loadPlayers(ctx context.Context, query *LotoPlayerQuery, nodes []*LotoGame, init func(*LotoGame), assign func(*LotoGame, *LotoPlayer)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*LotoGame)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(lotoplayer.FieldGameID)
	}
	query.Where(predicate.LotoPlayer(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(lotogame.PlayersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GameID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "game_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *LotoGameQuery) loadTickets(ctx context.Context, query *LotoTicketQuery, nodes []*LotoGame, init func(*LotoGame), assign func(*LotoGame, *LotoTicket)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*LotoGame)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(lototicket.FieldGameID)
	}
	query.Where(predicate.LotoTicket(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(lotogame.TicketsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GameID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "game_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *LotoGameQuery) loadPrizes(ctx context.Context, query *LotoPrizeQuery, nodes []*LotoGame, init func(*LotoGame), assign func(*LotoGame, *LotoPrize)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*LotoGame)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(lotoprize.FieldGameID)
	}
	query.Where(predicate.LotoPrize(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(lotogame.PrizesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GameID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "game_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *LotoGameQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *LotoGameQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lotogame.Table, lotogame.Columns, sqlgraph.NewFieldSpec(lotogame.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lotogame.FieldID)
		for i := range fields {
			if fields[i] != lotogame.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *LotoGameQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(lotogame.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = lotogame.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LotoGameGroupBy is the group-by builder for LotoGame entities.
type LotoGameGroupBy struct {
	selector
	build *LotoGameQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *LotoGameGroupBy) Aggregate(fns ...AggregateFunc) *LotoGameGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *LotoGameGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotoGameQuery, *LotoGameGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *LotoGameGroupBy) sqlScan(ctx context.Context, root *LotoGameQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LotoGameSelect is the builder for selecting fields of LotoGame entities.
type LotoGameSelect struct {
	*LotoGameQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *LotoGameSelect) Aggregate(fns ...AggregateFunc) *LotoGameSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *LotoGameSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotoGameQuery, *LotoGameSelect](ctx, _s.LotoGameQuery, _s, _s.inters, v)
}

func (_s *LotoGameSelect) sqlScan(ctx context.Context, root *LotoGameQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
